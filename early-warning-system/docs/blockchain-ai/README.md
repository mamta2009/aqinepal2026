# blockchain_AI — reference code on disk

**Markdown guides** moved to **`../guides/blockchain-ai/`** (`README.md`, `INTEGRATION_GUIDE.md`).

This folder keeps **ZIP mirrors and reference `.py`** files (`reference_ai_models_extended.py`, `reference_blockchain_logger.py`) aligned with **`../guides/blockchain-ai/`** narratives.
